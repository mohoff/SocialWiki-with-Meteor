- TERMINAL:
    * 'killmeteor' killt meteor-ghostinstaces, alias ist entsprechend angelegt

- DANGEROUS: with current heroname=>normalizedName-resolution, the every word in
  'hero.heroname' needs to start with a capital letter.

  ---------------------------------------------------------------
- lineup-creator-name noch irgendwo mit reinbringen? waere schon edel, aber wie
	userId auf username mappen?

- fix EDIT-button for lineups. Also implement lineupDetails-page.
	CHECK* go to heroDetails-page when clicked on a hero
	CHECK* implement headerrow with info "lineupName, lineupCreator"
!	* adapt voting mechanisms for lineup

- Lineups:
	CHECK*	bei insert+update pruefen, ob hero-names tatsaechlich welche sind, also
		als Hero existieren.
	CHECK* Hero-Reihenfolge bestimmen und automatisch bei insert/update festlegen.
	CHECK* bei einem Lineup-update verbieten, die helden zu updaten (?). Weil neue Helden
		eigentlich schon wieder ein neues Lineup sind.

- meteor #isolate nutzen

- platzierung der heroes auch in avatar mit unterbringen. Z.B. in bottom dunklem
	Kreis	der zentriert die jewelige Platzierung enthaelt.

- implement Search for "NEW" heroes. So you can type "new" in order to see all newly
	released heroes.

CHECK - for non-existing images we can use a fall-back background-image
	(e.g. maxsized subtle	cross)

- meteor pages als addon zur pagination passt nicht richtig.
  besser das "infinite scrolling" manuell implementieren, damit
  sorting besser kontrolliert werden kann!

- global constants.js anlegen, wo initialVotePower=5 und solche dinge festgelegt werden.

- offenbar mus sman sich keine geadnken ueber sanatizing/escapnig machen.
  user input wird ueber hdnlebars automatisch escaped

- meteor notify package/addon hinzufuegen. demo page ist in den firefox-bookmarks
  hinterlegt.

- collection avgGrowthstats anlegen, wo STR,AGI,INT-werte durchscnitte von allen
  helden (zugeordnet zu INT,AGI oder STR-typ) eingetragen werden, sodass diese
  als overlay in spidernet/radargraph angezeigt werden koennen.

- heroDetails > Synergies >> deleteRequests anlegen, wo registrierte User loeschungen von syenergy-entries
  beantragen koennen. ab gewissem treshold wird dieser deaktiviert
  (ueber status-feld)

- heroDetails > Synergies >> ueberlegen wie schadcode als eingabe vermieden
  werden kann.

- heroDetails > Synergies >> ueberlegen wie angezeigte anyahl von Synergies
  limitiert wird. und trotzdem auch neue addSynergies nicht untergehen...



CHECK - REDO IronRouter-stuff... apparently other syntax now.
--> and fix waitOn, data, ..., so that it really waits on the subscription
  dto be ready before processing Heroes-collection

CHECK - formatDate hours+minutes fix: alles <10 wird als single-digit angeyeigt,
  obwohl double-digit besser ist

- manche funktionalitaet (z.b. festlegen des initalVoteBonus)
  in onCreateAccount (o.ae.) implementieren. bisher alles in
  onLogin implementiert
  // server > server.js

- checken obs okay ist, 3 verschiedene new Dates() auszufuehren,
  obwohl alle 3 den gleichen Zeitpunkt darstellen sollen.
  Problem koennte runtime execution delay sein
  // server > server.js

- UTC vs. GMT+01 checken. aktuell wird glaube ich falsch gespeichert,
  da custom-methode convertDateToUTC() in lib > common.js
  // server > server.js


  /////////

- resets einbauen: (oder ists schon gemacht?), also fuer daily,monthly votes etcddd

- Router.configure.waitOn umsetzen. sodass userData nur 1x abgeholt wird,
  in Session geschrieben und von dort wieder abgeholt. sprich waitOn nicht
  fuer alle routen, sondern nur '/'-route. falls direkt auf andere route von
  user navigiert wird, wir von Session gelesen. falls nicht vorhanden, wird
  neu abgefragt und in session gespeichert.

- zwei lastLoginAts speichern aus den 2 vorangegangenen sessions, sodass user
  selbst bei f5 der aktuellen session, immer noch den vorangegangenen lastLoginAt
  sieht. Erst wenn onLogin wieder invoked wird, soll dieser wert geloescht werden,
  mit dem timestamp der session vor f5 ueberschrieben und der wert nach f5 in den
  neueren der beiden lsatLoginAts schreiben
    * lastLoginOld
    * lastLoginNew
  bei onLogin, schreibe New in Old und setze New auf currentTimestamp
