<template name="heroRankings">
  <h1>{{rankingCategory}} Ranking</h1>
  <div class="input-group searchWrapper">
		<span class="input-group-addon dateIconBg"><i class="glyphicon glyphicon-search"></i></span>
		<input type="text" class="form-control" id="search" placeholder="Commando, INT, Flying, ...">
	</div>
  <div class="container-fluid">
  {{#each heroes}}
    <div class="row herowrapper vertical-align">
      {{> heroRankingsElement}}
    </div>
  {{/each}}
  </div>
</template>
